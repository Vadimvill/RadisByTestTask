## 🚀 Запуск приложения

### Требования

- Docker
- Docker Compose

```bash
git clone <repository-url>
cd <project-directory>
создайте .env файл (в репозитории уже лежит шаблон)
docker-compose up --build (запуск используя docker-compose)
docker build -t {image_name} . (запуск используя docker)
docker run -p 8000:8000 {image_name} (запуск используя docker)
http://0.0.0.0:8000/docs -> документация swagger API
```

### Комментарии к заданию

# **Основные моменты реализации**

- **Слоистая структура**: FastAPI контроллеры → Use Cases → Внешний API
- **Абстрактные модели**: Единый интерфейс для всех схем данных
- **Разделение ответственности**: Отдельные модули для схем, бизнес-логики, утилит и middleware
- **Асинхронность**: async/await на всех уровнях
- **Обработка ошибок**: Middleware с обработкой 15+ типов ошибок HTTP/сети
- **Валидация**: Pydantic с фильтрацией None-значений и ограничениями
- **Безопасная конфигурация**: Ключи API через .env, изоляция настроек
- **Единая точка HTTP-запросов**: Переиспользуемая утилита httpx_request
- **Типизация через Enum**: Константы endpoints и типов запросов
- **Dependency Injection**: Параметры как зависимости в FastAPI
## **Трудности/проблемы при реализации**

### 1. **Специфика форматов RetailCRM API**
- **GET-запросы**: параметры через query string (`?filter[name]=...`)
- **POST-запросы**: `application/x-www-form-urlencoded` с JSON внутри
- **Решение**: Пришлось изучить документацию и создать разные методы сериализации

### 2. **Обработка None-значений в фильтрах**
- **Решение**: Реализована фильтрация None на уровне моделей Pydantic

### 3. **Обработка ошибок внешнего API**
- **Проблема**: Нужно было различать ошибки приложения и ошибки RetailCRM
- **Решение**: Создал middleware, который преобразует ошибки httpx в понятные HTTP-ответы

---